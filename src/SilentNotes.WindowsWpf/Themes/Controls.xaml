<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style TargetType="{x:Type Button}">
    <Setter Property="FontFamily" Value="{StaticResource SilentNotesUiFont}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="MinHeight" Value="32" />
    <Setter Property="Padding" Value="12,5" />
    <Setter Property="Foreground" Value="{DynamicResource SilentNotesTextBrush}" />
    <Setter Property="Background" Value="{DynamicResource SilentNotesPaperBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource SilentNotesBorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border x:Name="Root"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="5"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background" Value="{DynamicResource SilentNotesAccentSoftBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Root" Property="Background" Value="{DynamicResource SilentNotesAccentSoftBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45" />
              <Setter Property="Cursor" Value="Arrow" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="Foreground" Value="White" />
    <Setter Property="Background" Value="{DynamicResource SilentNotesPrimaryBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource SilentNotesPrimaryBrush}" />
  </Style>

  <Style x:Key="DangerButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="Foreground" Value="{DynamicResource SilentNotesDangerBrush}" />
    <Setter Property="Background" Value="{DynamicResource SilentNotesDangerSoftBrush}" />
    <Setter Property="BorderBrush" Value="#F4B9B4" />
  </Style>

  <Style x:Key="SegmentButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="MinHeight" Value="30" />
    <Setter Property="Padding" Value="10,4" />
  </Style>

  <Style x:Key="TagFilterButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="MinHeight" Value="26" />
    <Setter Property="MinWidth" Value="26" />
    <Setter Property="Padding" Value="8,3" />
    <Setter Property="FontSize" Value="11" />
    <Setter Property="Margin" Value="0,0,4,4" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border x:Name="Root"
                  Background="{DynamicResource SilentNotesPaperBrush}"
                  BorderBrush="{DynamicResource SilentNotesBorderBrush}"
                  BorderThickness="1"
                  CornerRadius="10"
                  SnapsToDevicePixels="True">
            <StackPanel Orientation="Horizontal" Margin="{TemplateBinding Padding}">
              <Path Data="M21.41,11.58L12.41,2.58A2,2,0,0,0,11,2H4A2,2,0,0,0,2,4V11A2,2,0,0,0,2.59,12.42L11.59,21.42A2,2,0,0,0,13,22A2,2,0,0,0,14.41,21.41L21.41,14.41A2,2,0,0,0,22,13A2,2,0,0,0,21.41,11.58M13,20L4,11V4H11L20,13M6.5,5A1.5,1.5,0,1,1,5,6.5A1.5,1.5,0,0,1,6.5,5Z"
                    Fill="{TemplateBinding Foreground}"
                    Width="12" Height="12"
                    Stretch="Uniform"
                    Margin="0,0,4,0"
                    VerticalAlignment="Center" />
              <ContentPresenter VerticalAlignment="Center" />
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background" Value="{DynamicResource SilentNotesAccentSoftBrush}" />
              <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource SilentNotesPrimaryBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="{x:Type TextBox}">
    <Setter Property="FontFamily" Value="{StaticResource SilentNotesUiFont}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="MinHeight" Value="32" />
    <Setter Property="Padding" Value="8,5" />
    <Setter Property="Foreground" Value="{DynamicResource SilentNotesTextBrush}" />
    <Setter Property="Background" Value="{DynamicResource SilentNotesPaperBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource SilentNotesBorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Style.Triggers>
      <Trigger Property="IsKeyboardFocusWithin" Value="True">
        <Setter Property="BorderBrush" Value="{DynamicResource SilentNotesPrimaryBrush}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style TargetType="{x:Type PasswordBox}">
    <Setter Property="FontFamily" Value="{StaticResource SilentNotesUiFont}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="MinHeight" Value="32" />
    <Setter Property="Padding" Value="8,5" />
    <Setter Property="Foreground" Value="{DynamicResource SilentNotesTextBrush}" />
    <Setter Property="Background" Value="{DynamicResource SilentNotesPaperBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource SilentNotesBorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Style.Triggers>
      <Trigger Property="IsKeyboardFocusWithin" Value="True">
        <Setter Property="BorderBrush" Value="{DynamicResource SilentNotesPrimaryBrush}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style TargetType="{x:Type RichTextBox}">
    <Setter Property="FontFamily" Value="{StaticResource SilentNotesUiFont}" />
    <Setter Property="Foreground" Value="{DynamicResource SilentNotesTextBrush}" />
    <Setter Property="Background" Value="{DynamicResource SilentNotesPaperBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource SilentNotesBorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="20" />
  </Style>

  <Style TargetType="{x:Type StatusBar}">
    <Setter Property="Background" Value="{DynamicResource SilentNotesPaperBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource SilentNotesBorderBrush}" />
    <Setter Property="BorderThickness" Value="0,1,0,0" />
    <Setter Property="MinHeight" Value="28" />
  </Style>

  <Style TargetType="{x:Type ToolBar}">
    <Setter Property="Background" Value="{DynamicResource SilentNotesPaperBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource SilentNotesBorderBrush}" />
    <Setter Property="BorderThickness" Value="0,1,0,1" />
  </Style>
</ResourceDictionary>
